# Ansible K3s Optimization

🚀 **Production-ready Ansible playbooks** для оптимизации K3s кластера, security hardening, honeypot deployment и автоматизации.

...

## 📊 Диагностика и аудит конфликтов

### Быстрая диагностика (CLI)

Для проверки всех типовых конфликтов и критических ошибок запускайте:
```bash
sudo /usr/local/bin/k3s-conflict-check
```

- Минималистичный информативный вывод
- Цветовая индикация и exit-код по критичности

### Глубокая диагностика (Ansible)

Полная проверка на все типы конфликтов, расхождений и локальных конфликтов:
```bash
ansible-playbook playbooks/diagnostic-comprehensive.yml
```

- Показывает причины проблем и рекомендации
- Категоризирует: ошибки (❌), предупреждения (⚠️), ОК (✅)
- Итоговый summary + подробные находки при необходимости

### Автоматические проверки

- Скрипт `/usr/local/bin/k3s-conflict-check` автоматически запускается каждое утро cron-ом
- Лог доступен: `/var/log/k3s-conflict-check.log`
- Ротация логов на 7 дней

### Установка инструментов диагностики

```bash
ansible-playbook playbooks/install-diagnostic-tools.yml
```

---

(Остальное описание репозитория без изменений)
...
